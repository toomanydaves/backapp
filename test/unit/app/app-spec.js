(function (require, describe, it) {
    'use strict'

    describe('app', function () {
        describe('setPage()', function () {
            describe('_state', function () { });
        });

        describe('openPage()', function () {
            describe('_pageViewer', function () { });
        });

        describe('sync()', function () {
            describe('_persistenceStrategy', function () { });
        });
    });
})(require, describe, it);
