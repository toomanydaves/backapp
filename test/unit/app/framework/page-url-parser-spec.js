(function (require, describe, it) {
    'use strict';

    describe('pageUrlParser', function () {
        describe('getPage()', function () {
            describe('_urlToPage()', function () { });
        });

        describe('getUrl()', function () { });

        describe('setUrlFromPage', function () {
            describe('_pageToUrl', function () { });
        });
    });
})(require, describe, it);
