(function (require, describe, it) {
    'use strict';

    describe('pageParser', function () {
        describe('getUrl()', function () { });

        describe('getLocation()', function () {
            describe('_urlToLocation()', function () { });
        });

        describe('setLocation()', function () {
            describe('_locationToUrl()', function () { });
        });
    });
})(require, describe, it);
